# Minimize

macOS 菜单栏常驻工具 — 鼠标滑到屏幕右上角右键单击，即可最小化或关闭窗口。

## 使用方式

1. 启动 App，菜单栏出现 `✕` 图标
2. 将鼠标移到屏幕右上角，会出现半透明高亮区域
3. 右键单击
   - **默认** → 最小化当前最上层窗口到 Dock
   - **设为「关闭」的 App** → 关闭窗口
   - **设为「不响应」的 App** → 无操作
4. 每次操作后自动聚焦到下一窗口，可连续操作

